{
  "name": "yle-etusivu",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w frontend",
    "build:shared": "npm run build -w shared",
    "dev:frontend": "npm run dev -w frontend",
    "start:backend": "npm run mikro-orm:migration:up && npm run dev -w backend",
    "mikro-orm:migration:up": "npm run mikro-orm:migration:up -w shared"
  }
}
